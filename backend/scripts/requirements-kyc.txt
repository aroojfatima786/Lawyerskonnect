# Primary face match (works on Python 3.11+ including 3.14)
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
# Optional (Python 3.11 + setuptools/pkg_resources); script falls back to OpenCV
face_recognition>=1.3.0
